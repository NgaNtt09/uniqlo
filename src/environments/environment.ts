// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  fisebase: {
    apiKey: "AIzaSyAour2TchgJcdTja_Gy1KzAZlFgdfaib2M",
    authDomain: "shopw-b3f7b.firebaseapp.com",
    databaseURL: "https://shopw-b3f7b.firebaseio.com",
    projectId: "shopw-b3f7b",
    storageBucket: "shopw-b3f7b.appspot.com",
    messagingSenderId: "118596433063"
 
  }
  
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
